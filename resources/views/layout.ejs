<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mie Aceh H. Abdullah</title>
    <link rel="shortcut icon" type="image/jpg" href="/img/favicon.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Lato&display=swap" rel="stylesheet" />
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/app.css" />
  </head>
  <body>
    <nav class="container mx-auto flex items-center justify-between py-4 px-10">
      <div>
        <img src="/img/locil.png" alt="logo" class="locil" />
      </div>
      <div>
        <ul class="flex items-center">
          <li class="ml-6 font-semibold"><a href="#">Menu</a></li>
          <li class="ml-6 font-semibold"><a href="#">Harga</a></li>
          <% if(user) { %>
          <li class="ml-6 font-semibold">
            <form id="logout" action="/logout" method="POST"><a onclick="document.getElementById('logout').submit()" href="#">Keluar</a></form>
          </li>
          <% } else { %>
          <li class="ml-6 font-semibold"><a href="/register">Daftar</a></li>
          <li class="ml-6 font-semibold"><a href="/login">Masuk</a></li>
          <% } %>
          <li class="ml-6">
            <a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">
              <span id="cartCounter" class="text-white font-medium pr-2"><%= session.cart ? session.cart.totalQty : '' %> </span>
              <img src="/img/cart.png" alt=""
            /></a>
          </li>
        </ul>
      </div>
    </nav>
    <%- body %>
    <footer></footer>
    <script src="/js/app.js"></script>
  </body>
</html>
